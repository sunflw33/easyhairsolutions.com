@extends('layouts.app')
@section('content')<div class="mx-auto max-w-7xl px-5 py-12 lg:px-8"><span class="badge">Owner dashboard</span><div class="mt-4 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><h1 class="font-display text-5xl text-deep">Good morning, {{ auth()->user()->name }}.</h1><p class="mt-3 text-gray-600">The operating view of your Easyhairsolutions business.</p></div><div class="flex gap-3"><a href="{{ route('booking.create') }}" class="btn-soft">Preview booking</a><a href="{{ route('shop') }}" class="btn-primary">View storefront</a></div></div><div class="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach([['Customers',$customers],['Professionals',$professionals],['Services',$services],['Products',$products]] as [$label,$value])<div class="card p-6"><p class="text-sm text-gray-500">{{ $label }}</p><p class="mt-2 text-4xl font-semibold text-deep">{{ $value }}</p></div>@endforeach</div><div class="card mt-5 p-6"><div class="flex items-center justify-between"><h2 class="text-lg font-semibold">Recent bookings</h2><span class="text-sm text-gray-400">Operations</span></div><div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="text-xs uppercase tracking-[.12em] text-gray-400"><tr><th class="py-3">Client</th><th>Service</th><th>Professional</th><th>When</th><th>Status</th></tr></thead><tbody class="divide-y divide-line">@foreach($appointments as $a)<tr><td class="py-4 font-medium">{{ $a->customer->name }}</td><td>{{ $a->service->name }}</td><td>{{ $a->professional->user->name }}</td><td>{{ $a->starts_at->format('M j · g:i A') }}</td><td><span class="badge">{{ $a->status }}</span></td></tr>@endforeach</tbody></table></div></div></div>@endsection
