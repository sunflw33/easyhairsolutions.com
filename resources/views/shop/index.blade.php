@extends('layouts.app')
@section('content')<div class="mx-auto max-w-7xl px-5 py-12 lg:px-8"><div class="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><span class="badge">Shop</span><h1 class="mt-4 font-display text-5xl tracking-[-.04em] text-deep">Salon essentials, curated.</h1><p class="mt-4 max-w-xl text-gray-600">A small collection of products that support the work between appointments.</p></div><a href="{{ route('cart.index') }}" class="btn-soft w-fit">View bag</a></div><div class="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">@foreach($products as $product)<article class="card overflow-hidden"><div class="aspect-[4/3] bg-[#eee9df]"></div><div class="p-5"><p class="text-xs font-semibold uppercase tracking-[.15em] text-sage">Easyhairsolutions essential</p><h2 class="mt-2 text-lg font-semibold">{{ $product->name }}</h2><p class="mt-2 min-h-10 text-sm leading-5 text-gray-500">{{ $product->description }}</p><div class="mt-5 flex items-center justify-between"><strong>${{ number_format($product->price,2) }}</strong><form method="POST" action="{{ route('cart.add',$product) }}">@csrf<button class="btn-soft px-4 py-2">Add to bag</button></form></div></div></article>@endforeach</div><div class="mt-8">{{ $products->links() }}</div></div>@endsection
