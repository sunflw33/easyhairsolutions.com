@extends('layouts.app')
@section('content')<div class="mx-auto max-w-5xl px-5 py-12 lg:px-8"><div class="max-w-2xl"><span class="badge">Your bag</span><h1 class="mt-4 font-display text-5xl tracking-[-.04em] text-deep">Products for the days between visits.</h1></div><form method="POST" action="{{ route('cart.update') }}" class="mt-10"><div class="grid gap-6 lg:grid-cols-[1fr_320px]"> <div class="card divide-y divide-line overflow-hidden">@forelse($products as $product)<div class="flex items-center gap-4 p-5"><div class="h-20 w-20 shrink-0 rounded-2xl bg-[#eee9df]"></div><div class="min-w-0 flex-1"><h2 class="font-semibold">{{ $product->name }}</h2><p class="mt-1 text-sm text-gray-500">${{ number_format($product->price,2) }}</p></div><input class="field w-20" type="number" min="0" max="99" name="qty[{{ $product->id }}]" value="{{ $cart[$product->id] ?? 0 }}"></div>@empty<div class="p-10 text-center text-gray-500">Your bag is empty. <a class="font-semibold text-sage" href="{{ route('shop') }}">Browse the shop →</a></div>@endforelse</div><aside class="card h-fit p-6"><span class="text-xs font-semibold uppercase tracking-[.15em] text-sage">Summary</span><div class="mt-5 flex justify-between"><span class="text-gray-500">Subtotal</span><strong>${{ number_format($total,2) }}</strong></div><button class="btn-soft mt-5 w-full">Update bag</button><a href="{{ route('checkout') }}" class="btn-primary mt-3 w-full text-center">Checkout</a></aside></div></form></div>@endsection
