@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-7xl px-5 py-12 lg:px-8"><div class="max-w-2xl"><span class="badge">Services</span><h1 class="mt-4 font-display text-5xl tracking-[-.04em] text-deep">Thoughtful services for real hair goals.</h1><p class="mt-4 text-gray-600">Clear pricing, realistic timing, and professionals who care about the health of your hair.</p></div><div class="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">@foreach($services as $item)<article class="card p-6"><div class="flex items-center justify-between"><span class="badge">{{ $item->category }}</span><span class="text-sm text-gray-500">{{ $item->duration_minutes }} min</span></div><h2 class="mt-5 font-display text-2xl text-deep">{{ $item->name }}</h2><p class="mt-3 text-sm leading-6 text-gray-600">{{ $item->description }}</p><div class="mt-7 flex items-center justify-between"><strong class="text-lg">${{ number_format($item->price,0) }}</strong><a class="btn-primary" href="{{ route('booking.create',['service'=>$item->id]) }}">Book</a></div></article>@endforeach</div></div>
@endsection
