@extends('layouts.app')
@section('content')<div class="mx-auto max-w-7xl px-5 py-12 lg:px-8"><span class="badge">Professional dashboard</span><div class="mt-4 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><h1 class="font-display text-5xl text-deep">Your day, your clients, your craft.</h1><p class="mt-3 text-gray-600">A focused workspace for upcoming appointments and client care.</p></div><a href="{{ route('services') }}" class="btn-soft">View service menu</a></div><div class="mt-10 grid gap-5 md:grid-cols-3"><div class="card p-6"><p class="text-sm text-gray-500">Upcoming</p><p class="mt-2 text-3xl font-semibold">{{ $appointments->count() }}</p></div><div class="card p-6"><p class="text-sm text-gray-500">Next appointment</p><p class="mt-2 text-lg font-semibold">{{ $appointments->first()?->starts_at?->format('M j, g:i A') ?? 'Open' }}</p></div><div class="card p-6"><p class="text-sm text-gray-500">Specialties</p><p class="mt-2 text-lg font-semibold">{{ implode(' · ', $professional?->specialties ?? []) ?: 'Add specialties' }}</p></div></div><div class="card mt-5 p-6"><h2 class="text-lg font-semibold">Upcoming appointments</h2><div class="mt-5 divide-y divide-line">@forelse($appointments as $appointment)<div class="flex flex-col gap-2 py-4 sm:flex-row sm:items-center sm:justify-between"><div><h3 class="font-semibold">{{ $appointment->service->name }}</h3><p class="text-sm text-gray-500">{{ $appointment->customer->name }} · {{ $appointment->starts_at->format('M j, Y · g:i A') }}</p></div><span class="badge">{{ $appointment->status }}</span></div>@empty<p class="py-5 text-sm text-gray-500">No upcoming appointments.</p>@endforelse</div></div></div>@endsection
