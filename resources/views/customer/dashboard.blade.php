@extends('layouts.app')
@section('content')<div class="mx-auto max-w-7xl px-5 py-12 lg:px-8"><div class="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><span class="badge">Customer dashboard</span><h1 class="mt-4 font-display text-5xl tracking-[-.04em] text-deep">Welcome back, {{ auth()->user()->name }}.</h1><p class="mt-3 text-gray-600">Everything you need for upcoming appointments and your hair routine.</p></div><a class="btn-primary" href="{{ route('booking.create') }}">Book again</a></div><div class="mt-10 grid gap-5 lg:grid-cols-[1.5fr_1fr]"><div class="card p-6"><div class="flex items-center justify-between"><h2 class="text-lg font-semibold">Your appointments</h2><span class="text-sm text-gray-400">{{ $appointments->count() }} shown</span></div><div class="mt-5 space-y-3">@forelse($appointments as $appointment)<div class="rounded-2xl border border-line p-4"><div class="flex flex-col justify-between gap-3 sm:flex-row"><div><p class="text-xs uppercase tracking-[.14em] text-sage">{{ $appointment->status }}</p><h3 class="mt-1 font-semibold">{{ $appointment->service->name }}</h3><p class="mt-1 text-sm text-gray-500">{{ $appointment->professional->user->name }} · {{ $appointment->starts_at->format('M j, Y · g:i A') }}</p></div><strong>${{ number_format($appointment->total,2) }}</strong></div></div>@empty<p class="rounded-2xl bg-cream p-5 text-sm text-gray-600">No appointments yet. Start with a service that fits your current hair goal.</p>@endforelse</div></div><div class="space-y-5"><div class="card p-6"><span class="text-xs uppercase tracking-[.14em] text-sage">Your next step</span><h2 class="mt-3 font-display text-3xl text-deep">Keep the routine going.</h2><p class="mt-3 text-sm leading-6 text-gray-600">Browse essentials selected for care between appointments.</p><a href="{{ route('shop') }}" class="btn-soft mt-5">Shop essentials</a></div><div class="card p-6"><h2 class="font-semibold">Profile</h2><p class="mt-2 text-sm text-gray-500">{{ auth()->user()->email }}</p></div></div></div></div>@endsection
